% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_list.R
\name{extract_class_list}
\alias{extract_class_list}
\title{Extract a subject-specific class list from Wonde API data}
\usage{
extract_class_list(
  secondary_data_raw,
  schools,
  secondary,
  maths_names,
  maths_desc_names,
  year_pattern,
  verbose = TRUE
)
}
\arguments{
\item{secondary_data_raw}{List of school-level data as returned by
\code{get_secondary_school_student_data()}.}

\item{schools}{Data frame of school metadata containing at least \code{id}, \code{name}, and \code{urn}.}

\item{secondary}{Data frame defining per-school extraction settings, including
\code{GetMathBySubject} and \code{GetMathByClassDescription}.}

\item{maths_names}{Character vector of subject names to match (used when
\code{GetMathBySubject = TRUE}).}

\item{maths_desc_names}{Character vector of text fragments to match in class
descriptions (used when \code{GetMathByClassDescription = TRUE}).}

\item{year_pattern}{Character or numeric pattern used to match student year codes
(e.g., `"7"` for Year 7 or `"8"` for Year 8).}

\item{verbose}{Logical; if \code{TRUE}, prints progress.}
}
\value{
A data frame combining all matched student, class, and teacher information
  across schools, or \code{NULL} if no valid data are found.
}
\description{
Extract a subject-specific class list from Wonde API data
}
\examples{
\dontshow{if (FALSE) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
class_list_df <- extract_class_list(
  secondary_data_raw = secondary_data_raw,
  schools = schools,
  secondary = secondary,
  maths_names = c("Mathematics", "Maths"),
  maths_desc_names = c("MATH", "NUMERACY"),
  year_pattern = "7"
)
\dontshow{\}) # examplesIf}
}
