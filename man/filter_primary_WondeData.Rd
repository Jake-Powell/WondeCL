% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filtering_wonde_data.R
\name{filter_primary_WondeData}
\alias{filter_primary_WondeData}
\title{Filter Primary Wonde Data by Year}
\usage{
filter_primary_WondeData(
  WondeData,
  years = c("0", "R", "Reception"),
  fallback_codes = NULL,
  verbose = TRUE
)
}
\arguments{
\item{WondeData}{A list of raw Wonde data (one element per school).}

\item{years}{Character vector of year identifiers to filter for,
for example `c("0", "R", "Reception")`.}

\item{fallback_codes}{Optional vector of fallback year codes to use when IDs do not align.
If `NULL`, defaults to the same as `years`.}

\item{verbose}{Logical; whether to print progress messages.}
}
\value{
A filtered list of Wonde data, with an `"issue_list"` attribute noting
schools where fallbacks were used.
}
\description{
Filters primary Wonde raw data to only include students, classes, and teachers
belonging to a selected National Curriculum year (for example Reception or Year 1).
}
\examples{
\dontrun{
reception_data <- filter_primary_WondeData(
  primary_data_raw,
  years = c("0", "R", "Reception")
)
}

}
